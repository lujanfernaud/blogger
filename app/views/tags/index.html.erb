<h1>Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
  	<li># <%= link_to tag.name, tag_path(tag) %><% if logged_in? %> · <%= link_to "Delete tag", tag_path(tag), method: :delete, data: {confirm: "Really delete the '#{tag.name}' tag?"} %><% end %></li>
  <% end %>
</ul>

<br />
<%= link_to "Articles List", articles_path %>
